# Claim 1 evaluation

Verdict: **VERIFIED**. Confidence: **HIGH**.

The universal symbolic certificate passed, the independent exact-rational
checker exhausted 418 answer profiles with zero mismatches, and the intended
conditional-dependence control produced `MAP=1` versus `OW=0`. See
`formal_result.json` and `run_metadata.json`.
